import { RecommendedHighlight } from './recommended-highlight';

describe('RecommendedHighlight', () => {
  it('should create an instance', () => {
    const directive = new RecommendedHighlight();
    expect(directive).toBeTruthy();
  });
});
